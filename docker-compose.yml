services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: devops-web
    ports:
      - "8080:80"
    restart: always
    volumes:
      - ./app:/usr/share/nginx/html